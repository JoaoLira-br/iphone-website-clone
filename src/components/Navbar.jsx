import { appleImg } from "../utils";

const Navbar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} alt="apple image" />
      </nav>
      Navbar
    </header>
  );
};

export default Navbar;
